package com.upgrade.meoku.data.repository;

import com.upgrade.meoku.data.entity.MeokuDetailedMenu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MeokuDetailedMenuRepository extends JpaRepository<MeokuDetailedMenu, Integer> {
}
